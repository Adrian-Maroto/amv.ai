{
  "name": "amv-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AMV.AI — single-file AI platform with modular source + test suite.",
  "scripts": {
    "build": "node build.mjs build",
    "split": "node build.mjs split",
    "dev": "node build.mjs dev",
    "check": "node build.mjs check",
    "test": "node tests/run-tests.mjs",
    "start": "npm run dev"
  },
  "engines": { "node": ">=18" },
  "devDependencies": { "playwright": "^1.40.0" }
}
